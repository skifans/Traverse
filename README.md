# Traverse

## How to run a local server?

1.  Download [node.js](https://nodejs.org/en/download/) to your computer.
2.  Open **Traverse** folder in _Command Line_ or _Terminal_.
3.  Use `node index.js` command.
4.  Open a browser and go to `http://localhost:3000`.
